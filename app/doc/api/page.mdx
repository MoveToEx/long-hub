import Typography from '@mui/material/Typography';
import Breadcrumbs from '@mui/material/Breadcrumbs';
import Link from '@mui/material/Link';

<Breadcrumbs aria-label="breadcrumb">
    <Typography color="text.primary">API</Typography>
</Breadcrumbs>

Endpoints below /:

- <Link color="inherit" underline="hover" href="/doc/api/post/">/api/post</Link>  
- <Link color="inherit" underline="hover" href="/doc/api/template/">/api/template</Link>  

Note that LONG Hub allows different image hosts when deployed in prod or dev mode.  
Therefore some pages in api document may use `localhost:3042` as the domain of its imageURL.  
